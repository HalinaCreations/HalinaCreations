services:
  halina-creations:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: halina-creations
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - RESEND_API_KEY=${RESEND_API_KEY}
